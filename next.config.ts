/** @type {import('next').NextConfig} */
const nextConfig = {
  // i18n: {
  //   locales: ['en', 'de'],
  //   defaultLocale: 'en',
  //   localeDetection: false, // optional: disable browser sniffing
  // },
};

module.exports = nextConfig;
